import { WindowResizedDirective } from './window-resized.directive';

describe('WindowResizedDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowResizedDirective();
    expect(directive).toBeTruthy();
  });
});
